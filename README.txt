To start the node server locally, navigate to project folder and:
> foreman start -p 5000

To install a new node module, from within node project folder:
> npm install <module name> --save

To start a local HTTP server and serve Backbone app, navigate to web project and:
> http-server
